---
phase: claude-code-for-business-course-offer-full-develop
bucket: J
milestone: claude-code-for-business
outcome: plan_delivered
date: 2026-06-06
---

# SUMMARY — Claude Code for Business (master plan, J phase)

**Plan-only phase. Deliverable = PLAN.md (the milestone blueprint), approved by Brady 2026-06-06.**

## What this phase produced
- **CONTEXT.md** — locked product decisions (two-layer model, owned Academy not Skool, gamification split by surface, warm-high-ticket-first pricing, validator as differentiator, two-sided referral).
- **PLAN.md** — full blueprint: product definition, curated 7-module curriculum spine (from CC101's 16), slide/HTML flow (workshop deck + Academy lesson template + sales page), 9 sequenced build phases, launch sequence, analytics events, marketing.
- **PLAN-REVIEW.md** — adversarial war-room (PROCEED_WITH_CHANGES). Key code-verified finding: Brightly Academy is operator-only internal training — no buyer-facing student portal/login/enrollment. Wk1 sale decoupled from Academy; deliver cohort #1 live + lab repo, record as taught.
- **PROGRESS.md** — tracks the 9 build phases (future /run phases off the milestone).
- 3 discuss-round research agents (pricing, Skool-vs-owned+mirror, gamification) + read of CC101 archive repo + brightly-crm academy code.

## Key decisions (see DECISIONS.md)
1. Two-layer product: Academy (hosted, sold) wraps CC101 (terminal lab, where they build).
2. Platform = owned Academy, not Skool (CRM moat). Free Skool = front-of-funnel later.
3. Gamification: Academy points/badges (public) + CC101 RPG (private lab); RPG A/B-validated in P1.
4. Pricing: warm high-ticket $997-1497 first → $297-497 self-serve + $39/mo → free validator + tripwire + ads.
5. Idea Validator = differentiator; built into /run + GitHub-downloadable skill + shareable score card.
6. War-room: decouple wk1 sale from Academy (Academy = operator-only, student portal is a build).
7. P1 CC101 rebuild = FULL up front (Brady override of light-touch); dedicated redo/optimization phase.

## Milestone roadmap (each = its own /run build phase)
P1 Curriculum rebuild (full, 🚀) → P2 Idea Validator → P3 Academy student portal + content (post-sale) → P4 Stripe link (wk1) + full checkout/onboarding (post-sale) → P5 sales page 🚀 → P6 workshop deck 🚀 → P7 referral → P8 self-serve tier → P9 scale (ads/tripwire/Skool).

## Wk1 cash path
Stripe Payment Link → P1 full CC101 rebuild → P5 sales page → P6 workshop → run live workshop → warm high-ticket close → deliver cohort live + lab repo (record as taught, zero pre-recorded video).

## Next
Kick off the first build phase with `/run` from this repo (e.g. `/run P5 sales page` or `/run rebuild the CC101 business lab`). Each inherits these locked decisions.
