---
phase: 04-test-out-system
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - CLAUDE.md
  - docs/claude/game-mechanics.md
autonomous: true

must_haves:
  truths:
    - "Challenge pass awards full module XP (200) and badge"
    - "Challenge pass adds module to completed.modules array"
    - "Challenge pass records module in challenges_passed array"
    - "Challenge failure shows specific feedback with retry/lesson options"
    - "Student can retry challenge immediately with no penalty"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Pass/fail handling patterns and reward calculations"
      contains: "Challenge Pass Celebration|Challenge Failure Feedback"
    - path: "docs/claude/game-mechanics.md"
      provides: "Challenge reward formulas"
      contains: "Challenge Completion"
  key_links:
    - from: "CLAUDE.md"
      to: "progress.json"
      via: "Updates challenges_passed and completed.modules"
      pattern: "challenges_passed.*\\[|completed\\.modules"
    - from: "docs/claude/game-mechanics.md"
      to: "CLAUDE.md"
      via: "Authoritative formulas referenced by teaching guide"
      pattern: "challenge.*XP|challenge.*Aura"
---

<objective>
Implement challenge validation engine, progress tracking, and pass/fail handling.

Purpose: Complete the test-out system by defining how challenges are validated, how results are recorded, and how pass/fail experiences feel. This ties together the challenge content from Plans 01-02 into a working system.

Output: Updated CLAUDE.md with validation patterns and progress tracking, updated game-mechanics.md with challenge reward formulas.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-test-out-system/04-CONTEXT.md
@.planning/phases/04-test-out-system/04-RESEARCH.md

# Will contain challenge content from 01-02
@CLAUDE.md

# Existing game mechanics formulas
@docs/claude/game-mechanics.md

# Visual templates for celebration patterns
@docs/claude/visual-templates.md

# Progress tracking schema
@progress.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Challenge Pass Celebration to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
In the Module Challenges section of CLAUDE.md, add a subsection "### Challenge Pass Celebration" that defines the victory experience.

Include:

1. **Celebration Template (VIS-04 variant):**
```
╔══════════════════════════════════════════╗
║   [trophy] CHALLENGE COMPLETE! [trophy]  ║
║                                          ║
║   Module {N}: {Module Name}              ║
║                                          ║
║   You proved your mastery! [lightning]   ║
║                                          ║
║   +200 XP | +10 Aura                     ║
║   Badge Earned: {Badge Name} [trophy]    ║
║                                          ║
║   Stats Boosted: {stat_emoji} {Stat} +3  ║
║                                          ║
║   Challenge time: {minutes} minutes      ║
╚══════════════════════════════════════════╝

[music_note] {random module_complete sequence}

You demonstrated existing knowledge and skipped ahead efficiently.
Your stats and rewards match the lesson path - you didn't miss anything!

Ready for Module {N+1}?
```
(Use actual emoji characters: trophy, lightning, music note)

2. **Music:** Same as module completion - random module_complete sequence (run_in_background: true)

3. **Progress Update Pattern (CRITICAL - single atomic write):**
   - Read progress.json ONCE
   - Calculate ALL updates:
     - Add module number to `completed.modules` array
     - Add module number (as string) to `challenges_passed` array (create if doesn't exist)
     - Add badge to `badges` array
     - Add +200 to `student.total_xp`
     - Add +3 to the module's stat (efficiency for M2, creativity for M3, etc.)
     - Add +10 to `aura_system.total_earned` AND `aura_system.current_balance`
     - Update `current_position.module` to N+1, `current_position.lesson` to 1, `current_position.task` to 1
     - Update `last_session` to today
   - Write progress.json ONCE

4. **Important notes:**
   - Challenge path does NOT add individual lessons/tasks to completed arrays (student skipped them)
   - The `challenges_passed` array tracks which modules were tested out (for analytics/display)
   - XP parity: 200 XP matches module completion bonus from lesson path
   - Stat parity: +3 matches module bonus (not per-task gains - this is the design decision from research)
   - Aura parity: +10 matches module completion Aura
  </action>
  <verify>
Read CLAUDE.md and confirm:
1. Challenge Pass Celebration subsection exists
2. Template matches VIS-04 style
3. Progress update pattern is documented with single read/write
4. All update fields listed (completed.modules, challenges_passed, badges, XP, stats, Aura, position)
  </verify>
  <done>
Challenge pass celebration defined in CLAUDE.md with VIS-04 variant template and atomic progress update pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Challenge Failure Feedback to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
In the Module Challenges section of CLAUDE.md, add a subsection "### Challenge Failure Feedback" that defines the kind failure experience.

Include:

1. **Close Attempt Template (passed some scenarios):**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[target] CHALLENGE RESULT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You're close! You got:
[checkmark] {passed_scenario_1}
[checkmark] {passed_scenario_2}
[x] {failed_scenario}

{Specific feedback on what needs review - 1-2 sentences}

Your options:
1. Type /challenge to retry (no XP penalty)
2. Type "continue" to take the lessons (comprehensive)
3. Type /hint for a concept refresher before retrying

Retrying after reviewing the concept is totally normal!
Many students retry 1-2 times.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

2. **Far From Passing Template (most scenarios failed):**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[target] CHALLENGE RESULT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This module covers several concepts you haven't
encountered yet. That's totally okay!

The challenge path is for students who've already
learned this material elsewhere. If Module {N} is new
to you, the lesson path is the best way to learn.

Your options:
1. Type "continue" to take the lessons (recommended)
2. Type /challenge to retry if you want another attempt

No penalty either way - the goal is learning, not racing!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

3. **Feedback guidelines:**
   - Be specific about what passed and what needs work
   - Never use shame or penalty language
   - Frame lessons as valuable alternative, not punishment
   - Normalize retrying ("many students retry 1-2 times")
   - No music on failure (keep it low-key)

4. **Retry policy:**
   - Unlimited immediate retries (no cooldown)
   - No XP penalty for failing or retrying
   - No tracking of attempt count (keep it simple)
   - If student retries 3+ times, gently suggest lesson path might be better fit

5. **Hint command:**
   - `/hint` during challenge shows a brief concept refresher (1-2 sentences per gap)
   - Not the full lesson content, just the key insight
   - Example for Module 2 npm question: "The -g flag installs packages globally, meaning you can use them from any folder on your computer."
  </action>
  <verify>
Read CLAUDE.md and confirm:
1. Challenge Failure Feedback subsection exists
2. Two templates: close attempt (specific feedback) and far from passing (encouraging)
3. Feedback guidelines documented
4. Retry policy is unlimited with no penalty
5. /hint command defined for concept refreshers
  </verify>
  <done>
Challenge failure feedback defined in CLAUDE.md with kind templates, retry policy, and hint support.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Challenge Rewards to game-mechanics.md</name>
  <files>docs/claude/game-mechanics.md</files>
  <action>
Add a new section to docs/claude/game-mechanics.md for challenge completion rewards.

Add section "## 8. Challenge Completion Rewards" (after existing sections):

```markdown
## 8. Challenge Completion Rewards

The test-out system awards rewards equivalent to module completion via lesson path.

### Challenge Pass Rewards

**XP Award:**
```
challenge_xp = 200  # Same as module completion bonus
```

Note: Challenge path does NOT award per-task XP (10 XP × N tasks). The 200 XP bonus is the total award. This is intentional - challenge proves mastery but doesn't replicate the learning journey's incremental rewards.

**Stat Award:**
```
challenge_stat_bonus = +3 to module's primary stat

Module stat tags:
  Module 2: Efficiency
  Module 3: Creativity
  Module 4: Efficiency
  Module 5: Creativity
  Module 6: Accuracy
  Module 7: Speed
```

Note: Challenge path does NOT award per-task stat growth (+1 per task). The +3 module bonus is the total stat award. This creates a slight stat advantage for lesson path (more total stats) while keeping XP parity. This encourages genuine learning while respecting existing knowledge.

**Aura Award:**
```
challenge_aura = +10  # Same as module completion

Updates both:
  aura_system.total_earned += 10
  aura_system.current_balance += 10
```

**Badge Award:**
```
Same badge as lesson path completion:
  Module 2: "Setup Champion 🏆"
  Module 3: "First Contact 🏆"
  Module 4: "Model Master 🏆"
  Module 5: "Prompt Engineer 🏆"
  Module 6: "Plan Mode Pro 🏆"
  Module 7: "Tech Foundation 🏆"
```

No badge variant for challenge path - same badge, same achievement.

### Challenge Failure Rewards

```
failure_xp = 0
failure_stats = 0
failure_aura = 0
failure_penalty = 0  # No negative consequences
```

Failure has no rewards but also no penalties. Student can retry immediately or take lesson path.

### Progress Tracking

**On challenge pass:**
```json
{
  "completed": {
    "modules": [1, 2],  // Add passed module
    "lessons": [...],   // Do NOT add skipped lessons
    "tasks": [...]      // Do NOT add skipped tasks
  },
  "challenges_passed": ["2"],  // Track separately (new field)
  "badges": ["Terminal Explorer 🏆", "Setup Champion 🏆"]
}
```

**challenges_passed array:**
- New field in progress.json schema
- Contains module IDs (as strings) that were completed via challenge
- Used for analytics and display ("You tested out of 3 modules!")
- Does not affect gameplay, purely tracking
```
  </action>
  <verify>
Read docs/claude/game-mechanics.md and confirm:
1. Section 8 exists for Challenge Completion Rewards
2. XP formula documented (200 XP, no per-task XP)
3. Stat formula documented (+3 module bonus only, no per-task stats)
4. Aura formula documented (+10)
5. Badge list for each module
6. Failure rewards documented (zero, no penalty)
7. Progress tracking with challenges_passed array explained
  </verify>
  <done>
game-mechanics.md contains authoritative challenge reward formulas and progress tracking documentation.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Read CLAUDE.md Module Challenges section - should have:
   - Challenge content for Modules 2-7 (from Plans 01-02)
   - Announcement pattern with topic summaries
   - Pass celebration with VIS-04 template
   - Failure feedback with kind templates
   - Retry policy and hint support
2. Read docs/claude/game-mechanics.md Section 8 - should have:
   - XP/stat/Aura formulas for challenge pass
   - Badge list
   - Failure rewards (zero, no penalty)
   - Progress tracking explanation
</verification>

<success_criteria>
1. Challenge pass awards 200 XP + 10 Aura + badge + 3 stat points
2. Challenge failure has no penalty, shows kind feedback
3. Retry is unlimited and immediate
4. challenges_passed array tracks test-outs
5. All reward formulas documented in game-mechanics.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-out-system/04-03-SUMMARY.md`
</output>
