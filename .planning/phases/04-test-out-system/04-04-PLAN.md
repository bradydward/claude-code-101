---
phase: 04-test-out-system
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - CLAUDE.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Student at module 2-7 start sees challenge announcement before lessons begin"
    - "Student typing /challenge triggers validation flow for current module"
    - "Challenge pass updates progress.json with challenges_passed tracking"
    - "New students get progress.json with challenges_passed array in schema"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Integrated challenge flow in Session Flow section"
      contains: "check for module start|On /challenge"
  key_links:
    - from: "Section 8 Session Flow"
      to: "Section 15 Module Challenges"
      via: "Challenge announcement at module start, /challenge handler"
      pattern: "At module start.*challenge|On.*challenge.*validation"
    - from: "Section 8a progress.json template"
      to: "Section 15 challenges_passed tracking"
      via: "Schema includes challenges_passed array"
      pattern: "challenges_passed.*\\[\\]"
---

<objective>
Integrate challenge system into teaching flow - wire announcements, /challenge handler, and progress.json schema.

Purpose: Close all 4 verification gaps by connecting the documented challenge system (Section 15) to the actual teaching flow (Section 8). The challenge content, validation approach, and reward formulas exist - this plan wires them into the session flow so students can actually use `/challenge`.

Output: Updated CLAUDE.md with integrated challenge flow in Session 8, updated progress.json schema in Section 8a.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-test-out-system/04-VERIFICATION.md

# The teaching guide with challenge content already documented
@CLAUDE.md

# Game mechanics with reward formulas
@docs/claude/game-mechanics.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add challenges_passed to progress.json Schema</name>
  <files>CLAUDE.md</files>
  <action>
In Section 8a (First Session Flow), update the progress.json template to include the `challenges_passed` array.

Find the "completed" object in the JSON template (around line 404-408) and add `challenges_passed` immediately after it:

```json
  "completed": {
    "modules": [],
    "lessons": [],
    "tasks": []
  },
  "challenges_passed": [],
```

This array will track module IDs (as strings) for modules completed via challenge path.
  </action>
  <verify>
Read CLAUDE.md Section 8a and confirm:
1. progress.json template contains `"challenges_passed": []`
2. Field is placed after "completed" object
  </verify>
  <done>
progress.json schema includes challenges_passed array for test-out tracking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Challenge Announcement to Session Flow</name>
  <files>CLAUDE.md</files>
  <action>
In Section 8 (Session Flow), modify step 7 to check for module start and display challenge announcement BEFORE presenting the task.

Replace step 7:
```
7. **Present current task** from curriculum.md
```

With:
```
7. **Check for module start (challenge announcement):**
   - If `current_position.lesson == 1` AND `current_position.task == 1` AND `current_position.module >= 2`:
     - Check if Module 1 is complete (1 in `completed.modules`)
     - If yes, display challenge announcement (see Section 15 - Challenge Announcement)
     - Wait for student response:
       - If `/challenge`: Go to "On /challenge" handler below
       - If "continue" or anything else: Proceed to step 8

8. **Present current task** from curriculum.md

9. **Teaching flow:**
   - Student practices OR Claude demonstrates (see Section 9)
   - Verify completion
   - Award XP and stats
   - On lesson complete: Update cheat sheet (see Section 9)
```

This renumbers steps 8-9 to accommodate the new step 7.
  </action>
  <verify>
Read CLAUDE.md Section 8 and confirm:
1. Step 7 now checks for module start and displays challenge announcement
2. Condition checks lesson==1, task==1, module>=2, and Module 1 complete
3. Branches to /challenge handler or continues to present task
4. Steps 8-9 are renumbered (present task, teaching flow)
  </verify>
  <done>
Session flow checks for module start and displays challenge announcement before presenting first task.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add /challenge Command Handler to Session Flow</name>
  <files>CLAUDE.md</files>
  <action>
In Section 8 (Session Flow), add a new subsection "### On /challenge" after the "On level up during session" subsection.

Add:

```markdown
### On /challenge

**Trigger:** Student types `/challenge` at a module start (Modules 2-7).

**Prerequisites check:**
1. Module 1 must be complete (check `completed.modules` includes 1)
2. Student must be at module start position (lesson 1, task 1)
3. Current module must be 2-7 (not Module 1)

If prerequisites not met, display:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ Challenge Not Available
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/challenge is only available:
• At the start of Modules 2-7
• After completing Module 1

Current position: Module {N}, Lesson {L}, Task {T}
{If Module 1 not complete: "Complete Module 1 first to unlock challenges."}
{If mid-module: "You're mid-module. Finish this module or start a new one to use /challenge."}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**If prerequisites met, run challenge validation:**

1. **Announce challenge start:**
   ```
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   🎯 MODULE {N} CHALLENGE
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Let's test your knowledge! I'll ask you {scenario_count} questions.

   Take your time - this usually takes 5-10 minutes.
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ```

2. **Execute validation scenarios** (from Section 15 - module-specific challenges):
   - For each scenario in the current module's challenge:
     - Present the scenario (automated/conversational/practical)
     - Record pass/fail based on criteria in Section 15
   - Track results: `passed_scenarios[]` and `failed_scenarios[]`

3. **Determine result:**
   - For Modules 2-6: Pass if ALL scenarios pass
   - For Module 7: Pass if 4 of 5 scenarios pass (see Section 15)

4. **On challenge PASS:**
   - Display Challenge Pass Celebration (see Section 15)
   - Update progress.json (atomic write - see Section 15 Progress Update Pattern):
     - Add module to `completed.modules`
     - Add module ID (as string) to `challenges_passed`
     - Add badge to `badges`
     - Add 200 XP, +3 stat, +10 Aura
     - Update `current_position` to next module start
   - Play module_complete music sequence (run_in_background: true)
   - Proceed to next module (back to step 7 with new module)

5. **On challenge FAIL:**
   - Display failure feedback (see Section 15 - close attempt or far from passing)
   - Wait for student choice:
     - `/challenge`: Restart challenge validation (step 1)
     - `/hint`: Show concept refresher for failed scenarios
     - "continue": Begin Lesson 1 of current module normally
```
  </action>
  <verify>
Read CLAUDE.md Section 8 and confirm:
1. "On /challenge" subsection exists after "On level up during session"
2. Prerequisites check documented (Module 1 complete, at module start, module 2-7)
3. Validation flow references Section 15 scenarios
4. Pass handling includes progress update and celebration
5. Fail handling includes retry, hint, and continue options
  </verify>
  <done>
/challenge command handler integrated into Session Flow with validation, pass, and fail handling.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Read CLAUDE.md Section 8a - confirm challenges_passed in progress.json template
2. Read CLAUDE.md Section 8 steps 7-9 - confirm challenge announcement at module start
3. Read CLAUDE.md Section 8 "On /challenge" - confirm full handler with prerequisites, validation, pass/fail
4. Verify Section 15 references are correct (Challenge Announcement, validation scenarios, celebration, failure feedback)
</verification>

<success_criteria>
1. progress.json schema includes `challenges_passed: []` array
2. Session flow step 7 checks for module start and displays challenge announcement
3. /challenge handler executes validation scenarios from Section 15
4. Challenge pass triggers atomic progress update and celebration
5. Challenge fail offers retry, hint, or continue options
6. All 4 verification gaps from 04-VERIFICATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-out-system/04-04-SUMMARY.md`
</output>
