---
phase: 05-guided-project-track
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - curriculum.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Lessons have project_types and skip_if fields for routing"
    - "Irrelevant lessons auto-skip with 10 XP efficiency bonus"
    - "Lesson examples use student's project variables (YOUR_APP_NAME, etc.)"
    - "Curriculum router determines lesson relevance based on project type"
  artifacts:
    - path: "curriculum.md"
      provides: "Project type routing metadata on lessons"
      contains: "project_types:"
    - path: "CLAUDE.md"
      provides: "Curriculum routing logic section"
      contains: "Curriculum Router"
  key_links:
    - from: "CLAUDE.md Curriculum Router"
      to: "curriculum.md lesson metadata"
      via: "project_types and skip_if fields"
      pattern: "skip_if.*static_site"
    - from: "CLAUDE.md lesson flow"
      to: "project.json contextualization_vars"
      via: "variable substitution"
      pattern: "YOUR_APP_NAME"
---

<objective>
Implement curriculum routing that adapts lessons to student's project type.

Purpose: Students building a static portfolio shouldn't learn database lessons. Students building a recipe app should see their actual project in lesson examples, not generic "progress.json" examples. Routing makes every lesson relevant.

Output:
- curriculum.md enhanced with project_types, skip_if, contextualize_as fields
- CLAUDE.md section for curriculum routing logic
- Variable substitution pattern for project-specific examples
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-guided-project-track/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add routing metadata to curriculum.md lessons</name>
  <files>curriculum.md</files>
  <action>
Read curriculum.md and add metadata to ALL lessons (Modules 1-15). Use this format at the end of each lesson header:

```markdown
### Lesson X.Y: [Title]
**project_types:** [all | list of types]
**skip_if:** [list of project types to skip for]
**contextualize_as:** { "crud_app": "context text", "api_consumer": "context text" }
```

Apply these routing rules:

**Universal lessons (project_types: all):**
- Module 1: All lessons (terminal basics - everyone needs these)
- Module 2: All lessons (installing Claude Code - everyone needs this)
- Module 3: Lessons 3.1-3.3 (basic conversation skills)
- Module 5: All lessons (prompting - everyone benefits)
- Module 6: All lessons (plan mode - everyone benefits)

**CRUD-focused lessons (skip_if: static_site, game, utility_tool):**
- Module 7: Understanding JSON (contextualize for data storage)
- Module 9: File operations with data

**API-consumer lessons (skip_if: static_site, crud_app, game, utility_tool):**
- Any fetch/external API lessons

**Contextualization examples for common lessons:**

Module 7 (JSON):
```markdown
**contextualize_as:** {
  "crud_app": "Storing YOUR_DATA_PLURAL as JSON - like your actual YOUR_APP_NAME data",
  "api_consumer": "Parsing API responses for YOUR_APP_NAME"
}
```

Module 9 (Files):
```markdown
**contextualize_as:** {
  "crud_app": "Managing YOUR_DATA_TYPE files in YOUR_APP_FOLDER",
  "static_site": "Organizing pages for YOUR_APP_NAME"
}
```

For lessons WITHOUT specific contextualization, leave contextualize_as empty or omit.

IMPORTANT: Add to at least the first 7 modules (21+ lessons) to demonstrate the pattern. Focus on lessons that have clear project-type relevance.
  </action>
  <verify>
Read curriculum.md and verify:
1. At least 21 lessons have project_types metadata
2. At least 5 lessons have skip_if rules
3. At least 3 lessons have contextualize_as objects
4. Module 1-3 lessons are marked as "all" project types
  </verify>
  <done>curriculum.md lessons enhanced with project_types, skip_if, and contextualize_as metadata</done>
</task>

<task type="auto">
  <name>Task 2: Document curriculum routing logic in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add to Section 16 (Guided Project Mode) a new subsection:

```markdown
### Curriculum Router

When in guided project mode (guided_project.active = true), check lesson relevance before presenting.

**Routing Logic:**

1. Read lesson metadata from curriculum.md (project_types, skip_if, contextualize_as)
2. Get student's project_type from progress.json.guided_project.project_type

**Decision Flow:**

```
If lesson.project_types includes "all":
  -> Show lesson (universal content)

Else if lesson.skip_if includes student.project_type:
  -> Auto-skip with efficiency bonus (see below)

Else if lesson.project_types includes student.project_type:
  -> Show lesson with contextualization (if available)

Else:
  -> Show lesson as-is (default: include)
```

**Auto-Skip Display (10 XP efficiency bonus):**

When skipping an irrelevant lesson:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[skip_emoji] LESSON SKIPPED: [Lesson Title]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This lesson covers [topic] which isn't needed
for your [project_type] project ([project_name]).

You earned the progression anyway:
+10 XP (efficiency bonus for focused learning)

Moving to the next relevant lesson...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Update progress.json:
- total_xp += 10
- Append lesson to completed.lessons (mark as skipped)
- DO NOT award stat points (skipped lessons don't build skills)

Play Pop.aiff (run_in_background: true) for the skip.

**Contextualization Display:**

When showing a contextualized lesson:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[lesson_emoji] FOR YOUR PROJECT: [Project Name]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[contextualize_as text with variables substituted]

Let's learn this by building part of YOUR_APP_NAME...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Then proceed with lesson tasks, using project context throughout.
```
  </action>
  <verify>
Read CLAUDE.md Section 16 and verify:
1. Curriculum Router subsection exists
2. Decision flow documented (all, skip_if, project_types, default)
3. Auto-skip display template included with 10 XP bonus
4. Contextualization display template included
5. progress.json update pattern for skipped lessons documented
  </verify>
  <done>Curriculum routing logic documented with skip and contextualization patterns</done>
</task>

<task type="auto">
  <name>Task 3: Add variable substitution pattern for project examples</name>
  <files>CLAUDE.md</files>
  <action>
Add to Section 16 (Guided Project Mode) a new subsection:

```markdown
### Variable Substitution in Lessons

When teaching in guided project mode, substitute these variables with student's project data:

| Variable | Source | Example |
|----------|--------|---------|
| YOUR_APP_NAME | project.json.project_name | "Recipe Keeper" |
| YOUR_APP_FOLDER | derived (lowercase, hyphens) | "recipe-keeper" |
| YOUR_DATA_TYPE | derived from project type | "recipe" |
| YOUR_DATA_PLURAL | derived from project type | "recipes" |

**Data Type Derivation:**

| Project Type | YOUR_DATA_TYPE | YOUR_DATA_PLURAL |
|--------------|----------------|------------------|
| crud_app | "item" or first V1 feature noun | "items" or plural |
| api_consumer | "result" | "results" |
| game | "score" | "scores" |
| utility_tool | "output" | "outputs" |
| static_site | "page" | "pages" |

For crud_app, attempt to extract noun from first V1 feature:
- "Save recipes" -> recipe/recipes
- "Track expenses" -> expense/expenses
- "Manage tasks" -> task/tasks

**Substitution Examples:**

Generic lesson text:
> "Create a file called example.json with your data"

Contextualized for Recipe Keeper:
> "Create a file called recipe.json with your recipe data"

Generic:
> "Open progress.json and add a new field"

Contextualized:
> "Open recipe-keeper/data.json and add a new recipe"

**Teaching Pattern:**

When presenting lesson content in guided project mode:
1. Read lesson text
2. Replace all YOUR_* variables with project values
3. Present contextualized version
4. Create ACTUAL project files (recipe.json, not example.json)

The student builds their REAL project during lessons, not throwaway examples.
```
  </action>
  <verify>
Read CLAUDE.md Section 16 and verify:
1. Variable substitution table exists (YOUR_APP_NAME, YOUR_APP_FOLDER, YOUR_DATA_TYPE, YOUR_DATA_PLURAL)
2. Data type derivation table by project type
3. Substitution examples showing before/after
4. Teaching pattern for applying substitution
  </verify>
  <done>Variable substitution pattern documented for project-specific lesson examples</done>
</task>

</tasks>

<verification>
After all tasks:
1. Read curriculum.md - verify project routing metadata on multiple lessons
2. Read CLAUDE.md Section 16 - verify Curriculum Router subsection
3. Verify auto-skip gives 10 XP but no stat points
4. Verify variable substitution table is complete
5. Verify contextualization examples show clear before/after
</verification>

<success_criteria>
1. curriculum.md has project_types, skip_if, contextualize_as on 20+ lessons
2. Curriculum router logic documented with clear decision flow
3. Auto-skip awards 10 XP efficiency bonus
4. Variable substitution documented with derivation rules
5. Lesson examples show transformation from generic to project-specific
</success_criteria>

<output>
After completion, create `.planning/phases/05-guided-project-track/05-02-SUMMARY.md`
</output>
