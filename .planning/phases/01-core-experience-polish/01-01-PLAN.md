---
phase: 01-core-experience-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["CLAUDE.md"]
autonomous: true

must_haves:
  truths:
    - "Teaching instructions never reference two terminals or window switching"
    - "Claude can demonstrate commands via Bash tool as teaching examples"
    - "Student can run commands and share results in the same conversation"
    - "Verification happens via Read/Glob/Grep tools regardless of who ran the command"
    - "Teaching tone is collaborative (Let's do this) not directive (Go do this)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Single-conversation teaching pattern, celebration sequence instructions, verification patterns"
      contains: "Single Conversation Pattern"
  key_links:
    - from: "CLAUDE.md Section 17"
      to: "curriculum.md task format"
      via: "teaching pattern defines how tasks are presented"
      pattern: "Single Conversation Pattern"
    - from: "CLAUDE.md Section 16"
      to: "progress.json updates"
      via: "session flow defines when progress is updated"
      pattern: "Read ONCE.*Calculate ALL.*Write ONCE"
---

<objective>
Convert CLAUDE.md teaching pattern from two-terminal to single-conversation, and ensure error recovery guidance and verification patterns are documented.

Purpose: The two-terminal pattern creates confusion for beginners and doesn't match how curriculum.md tasks are written. Single conversation (student runs `claude` and all teaching happens there) is simpler, collaborative, and matches reality.

Output: Updated CLAUDE.md with rewritten Section 17 (How to Teach), Section 16 (Session Flow), and removal of all two-terminal references. Error recovery guidance and verification patterns clearly documented.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-experience-polish/01-CONTEXT.md

Key reference: /Users/bradyward/.claude/plans/keen-sniffing-quail.md contains the detailed conversion plan with before/after examples.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md Teaching Pattern (Sections 16 and 17)</name>
  <files>CLAUDE.md</files>
  <action>
  Read CLAUDE.md fully. Then rewrite these sections:

  **Section 17 (How to Teach) - Complete Rewrite:**
  - Remove "CRITICAL: Two Terminal Windows" subsection entirely
  - Remove all 🖥️/💬 emoji format examples
  - Replace with "Single Conversation Pattern" subsection:
    - Student runs `claude` in project folder, all teaching happens there
    - Two modes: "Student-Led Practice" (student types command, shares result) and "Claude-Demonstrated" (Claude runs via Bash tool to show)
    - Choose mode based on teaching moment (early lessons = more demo, later = more student practice)
    - Tone shift: collaborative "Let's check..." not directive "Go type..."
  - Keep existing subsections: Verifying Completion, Awarding XP, Handling Questions, Handling Mistakes, Handling "I'm stuck"
  - Update "Presenting Tasks" to remove window-specific instructions
  - Add "Verification Strategy" subsection: Claude uses Read/Glob/Grep tools to confirm file/command results regardless of who ran the command
  - Add "Error Recovery" subsection: When student encounters errors, normalize it, explain in plain language, guide fix step by step, award +1 stat for learning from mistakes

  **Section 16 (Session Flow) - Update:**
  - Remove any two-terminal references
  - Update "On start lesson" flow step 7: "Present current task from curriculum.md" (remove window context)
  - Ensure flow includes: present task -> student practices OR Claude demonstrates -> verify -> celebrate -> next task

  **Search and Remove Throughout Entire File:**
  - Any remaining "practice terminal" or "conversation window" references
  - Any "🖥️ IN YOUR PRACTICE TERMINAL" or "💬 THEN BACK HERE" patterns
  - Any instructions about opening a second terminal window
  - Replace "the separate window" type language with "in this conversation" or remove
  </action>
  <verify>
  - `grep -c "practice terminal\|PRACTICE TERMINAL\|conversation window\|two terminal\|Two Terminal\|🖥️\|💬.*BACK HERE" CLAUDE.md` returns 0
  - `grep -c "Single Conversation Pattern" CLAUDE.md` returns at least 1
  - `grep -c "Error Recovery\|error recovery" CLAUDE.md` returns at least 1
  - Section 17 contains both "Student-Led Practice" and "Claude-Demonstrated" patterns
  </verify>
  <done>
  CLAUDE.md contains zero two-terminal references. Section 17 documents single-conversation teaching with both student-led and Claude-demonstrated modes. Error recovery guidance is documented. Verification uses file tools (Read/Glob/Grep). Teaching tone is collaborative throughout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Consistency and Remove Stale References</name>
  <files>CLAUDE.md</files>
  <action>
  After Task 1's rewrite, do a full consistency pass on CLAUDE.md:

  1. Search entire file for any remaining references to:
     - "other window", "second window", "separate terminal"
     - "switch to", "go back to" (in terminal context)
     - Any formatting that implies two places to type

  2. Check Section 22 (Web Onboarding Awareness) - update point 1:
     - Currently says "Frame Module 1 as reinforcement"
     - Should NOT reference two terminals. Update if it does.

  3. Verify Section 21 (Performance Optimization):
     - Atomic progress pattern still documented
     - Music run_in_background pattern still documented
     - These should NOT have changed

  4. Verify Section 9 (Visual System):
     - Status display format is intact
     - Level-up animation format is intact
     - Celebrations hierarchy (task/lesson/module) is clear

  5. Verify coherence: The document should read as if two-terminal was never a thing. Natural, single-conversation flow throughout.
  </action>
  <verify>
  - Full file grep for two-terminal concepts returns 0 matches
  - Section 21 still contains "Read progress.json ONCE" pattern
  - Section 9 still contains the level-up box-drawing example
  - File is valid markdown (no broken formatting from edits)
  </verify>
  <done>
  CLAUDE.md is internally consistent. No stale two-terminal references remain. Performance patterns, visual system, and celebration formats all intact. Document reads naturally as a single-conversation teaching guide.
  </done>
</task>

</tasks>

<verification>
- No two-terminal references exist in CLAUDE.md (grep returns 0)
- Single Conversation Pattern documented with examples
- Error recovery guidance documented
- Verification strategy documented (Read/Glob/Grep tools)
- Performance optimization patterns preserved
- Visual celebration formats preserved
- Teaching tone is collaborative, not directive
</verification>

<success_criteria>
A fresh Claude instance reading this CLAUDE.md would teach using a single conversation with no confusion about terminal setup. It would know how to verify student work, recover from errors, and present tasks collaboratively.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-experience-polish/01-01-SUMMARY.md`
</output>
