---
phase: 02-onboarding-a-flow
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - CLAUDE.md
  - docs/claude/game-systems.md
  - docs/claude/shop-system.md
autonomous: true

must_haves:
  truths:
    - "Student typing 'skills' or 'shop' before unlock sees friendly locked message with unlock condition"
    - "Skill tree unlocks after Module 3 completion (with celebration)"
    - "Shop unlocks after Module 6 completion (with celebration)"
    - "Sandbox unlocks at Level 5 (with celebration)"
    - "Web portal student hears acknowledgment of prior practice"
    - "Web portal student gets 'remember X from the portal?' phrasing in Module 1"
    - "Feature unlock celebrations use VIS-01 style (bordered box)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Progressive disclosure logic and web portal recognition"
      contains: "feature_unlocks"
    - path: "docs/claude/game-systems.md"
      provides: "Feature unlock table and communication patterns"
      contains: "Feature Unlock"
    - path: "docs/claude/shop-system.md"
      provides: "Shop locked-state handling"
      contains: "shop_unlocked"
  key_links:
    - from: "CLAUDE.md"
      to: "progress.json"
      via: "feature_unlocks boolean checks"
      pattern: "feature_unlocks\\.(skill_tree|shop|sandbox)_unlocked"
    - from: "CLAUDE.md"
      to: "docs/claude/game-systems.md"
      via: "@-reference for unlock details"
      pattern: "game-systems"
---

<objective>
Implement progressive disclosure (locked features with friendly messages, celebration on unlock) and web portal acknowledgment (recognition of prior practice, adapted teaching tone). Update CLAUDE.md command handlers, game-systems.md with unlock table, and shop-system.md with locked-state behavior.

Purpose: ONBD-03 (web portal progress acknowledged) and ONBD-04 (progressive disclosure - skill tree Module 3, shop Module 6). Features reveal themselves when students have context to understand them, and web portal students feel their effort was recognized.
Output: Updated teaching instructions with disclosure gates and portal recognition patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-onboarding-a-flow/02-CONTEXT.md
@.planning/phases/02-onboarding-a-flow/02-RESEARCH.md
@CLAUDE.md
@docs/claude/game-systems.md
@docs/claude/shop-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add progressive disclosure logic to CLAUDE.md and game-systems.md</name>
  <files>CLAUDE.md, docs/claude/game-systems.md</files>
  <action>
**In CLAUDE.md:**

1. Find Section 10 ("Key Commands"). Update the command table entries for /skills, /shop, and /sandbox to include lock-state behavior:

Replace or augment these rows:
```markdown
| "skills" / "/skills" | Show skill tree (if unlocked) OR locked message |
| "shop" / "/shop" | Browse cosmetics (if unlocked) OR locked message |
| "sandbox" / "/sandbox" | Enter sandbox mode (if unlocked) OR locked message |
```

2. Add a new section "8b. Progressive Disclosure" after Section 8a (First Session Flow), before Section 9:

```markdown
---

## 8b. Progressive Disclosure

Features unlock progressively as students gain context to understand them. Never show locked features in status displays or mention them in teaching until unlocked.

### Feature Unlock Table

| Feature | Unlock Trigger | Why Hidden Until Then |
|---------|----------------|----------------------|
| Skill Tree | Module 3 complete | Needs class selection (M3.L4) and level-up experience |
| Shop | Module 6 complete | Needs ~60+ Aura to afford items, needs class context |
| Sandbox | Level 5 | Needs mastery of basics before free experimentation |

### Locked Feature Response

When student types a command for a locked feature, respond with:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔒 [Feature Name] - Locked
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Unlocks after [condition]!

[One sentence of encouragement about current progress]

Keep going - you'll get there soon!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Examples:
- Skills: "Unlocks after Module 3! You'll choose your class and start spending skill points."
- Shop: "Unlocks after Module 6! You're earning Aura with every task - soon you'll spend it on cosmetics."
- Sandbox: "Unlocks at Level 5! Master the basics first, then experiment freely."

### Unlock Celebration

When a feature unlocks (module complete or level reached), check `feature_unlocks` in progress.json. If a new unlock is available:

1. Set the flag: `feature_unlocks.[feature]_unlocked = true`
2. Play Hero.aiff (run_in_background: true)
3. Display unlock celebration:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 NEW FEATURE UNLOCKED!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ [Feature Name] is now available!

[Brief description of what it does]

Type "[command]" to try it out!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### When to Check Unlocks

Check feature unlocks at these moments:
- After module completion (check skill_tree and shop)
- After level-up (check sandbox)
- NEVER proactively mention locked features in teaching
- NEVER show locked features in status displays

### Status Display Filtering

In the status display (Section 8, step 4):
- Only show Skill Tree section if `feature_unlocks.skill_tree_unlocked` is true
- Only show Shop/Aura section if `feature_unlocks.shop_unlocked` is true
- Only show Sandbox option if `feature_unlocks.sandbox_unlocked` is true
```

**In docs/claude/game-systems.md:**

Read the file. Find where features/systems are listed. Add a new section "Feature Unlock Schedule" (after the Character Classes section or after Progression):

```markdown
## Feature Unlock Schedule

Features reveal progressively to avoid overwhelming new students. Each feature unlocks when the student has enough context to understand it.

| Feature | Unlock Trigger | progress.json Field | Command |
|---------|----------------|---------------------|---------|
| Skill Tree | Module 3 complete | `feature_unlocks.skill_tree_unlocked` | `skills` or `/skills` |
| Cosmetics Shop | Module 6 complete | `feature_unlocks.shop_unlocked` | `shop` or `/shop` |
| Sandbox Mode | Level 5 reached | `feature_unlocks.sandbox_unlocked` | `sandbox` or `/sandbox` |

### Unlock Communication

**Before unlock (student tries command):**
Show friendly "locked" message with unlock condition and encouragement.

**On unlock (milestone reached):**
Show celebration banner with feature description and command hint.

**After unlock:**
Feature works normally. Include in status displays.

### Why These Thresholds

- **Skill Tree at Module 3:** Class selection happens in Module 3, Lesson 4. Skills require understanding of stats and class bonuses. Before Module 3, skill points have no context.
- **Shop at Module 6:** Students need ~60+ Aura to afford basic items. Module 6 gives enough accumulated Aura for first purchase. Also needs class context for class-locked items.
- **Sandbox at Level 5:** Free experimentation requires confidence in basics. Level 5 (~400 XP) means student has completed enough tasks to self-direct.
```
  </action>
  <verify>
Read CLAUDE.md and verify:
- Section 8b exists with progressive disclosure logic
- Locked feature response template exists
- Unlock celebration template exists
- Status display filtering rules exist
- Key Commands table updated for lock-state behavior

Read docs/claude/game-systems.md and verify:
- "Feature Unlock Schedule" section exists
- Table lists all 3 features with triggers and progress.json fields
  </verify>
  <done>
CLAUDE.md has progressive disclosure logic (Section 8b) with locked responses, unlock celebrations, and status filtering. game-systems.md documents the unlock schedule with thresholds and rationale.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update web portal recognition and shop locked-state</name>
  <files>CLAUDE.md, docs/claude/shop-system.md</files>
  <action>
**In CLAUDE.md Section 14 (Web Onboarding Awareness):**

Read CLAUDE.md, find Section 14. Replace or expand it with enhanced web portal recognition:

```markdown
## 14. Web Onboarding Awareness

Students may arrive from web portal (web/terminal.html) having practiced:
- `echo`, `pwd`, `ls`, `cd`, `mkdir`, `claude` (simulated)

### Detection

The web portal question is asked in Section 8a, Step 5. If student answers yes, `onboarding.from_web_portal` is set to true.

Additional detection: If student mentions "web portal", "practice terminal", "browser version", or "tutorial online" at any point, set the flag and acknowledge.

### Acknowledgment Template

When `onboarding.from_web_portal` is true, display:

```
Hey [Name]! You already conquered the web portal! 🎉

You practiced:
  echo (talk to the computer)
  pwd (where am I?)
  ls (what's here?)
  cd (move around)
  mkdir (create folders)

That was practice mode. Now you're in the REAL terminal -
same commands, but they actually change your computer.

Module 1 covers the same basics for real. Since you've
practiced, it should feel familiar and fast!
```

### Teaching Adjustments (When from_web_portal is true)

1. **Use "remember from the portal" phrasing:**
   - "You remember pwd from the portal? Let's try it on your real Mac..."
   - "echo worked the same way in the browser - try it here..."

2. **Skip fear-reduction language:**
   - DO NOT say: "Don't worry, the terminal won't bite"
   - DO say: "You've typed these before - same thing, just real this time"

3. **Move slightly faster through Module 1:**
   - Shorter explanations for echo, pwd, ls, cd, mkdir
   - Focus on "this is real now" rather than "this is what it does"
   - Still verify each task (they need to do it on real system)

4. **Never transfer XP:**
   - Portal XP (~120) does NOT carry over
   - Acknowledge effort: "Your practice paid off - Module 1 will fly by"
```

**In docs/claude/shop-system.md:**

Read the file. Find the "Shop Triggers" section (where it lists the commands that open the shop). Add a "Locked State" subsection before or after Shop Triggers:

```markdown
### Locked State (Before Module 6)

If `feature_unlocks.shop_unlocked` is false when student triggers a shop command:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔒 Cosmetics Shop - Locked
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Unlocks after Module 6!

You're earning Aura with every task (+1 per task).
After Module 6, you'll have enough to start shopping
for character skins, aura colors, and themes.

Current Aura balance: [read from progress.json]

Keep going - you'll be shopping soon!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Do NOT show any shop items, categories, or prices while locked. Only show the locked message with current Aura balance as a teaser.
```
  </action>
  <verify>
Read CLAUDE.md Section 14 and verify:
- Acknowledgment template exists with list of practiced commands
- 4 teaching adjustments listed (portal phrasing, skip fear-reduction, faster pace, no XP transfer)
- Detection method documented

Read docs/claude/shop-system.md and verify:
- "Locked State" subsection exists
- Locked message template shown with Aura balance teaser
- Clear instruction to NOT show shop items while locked
  </verify>
  <done>
Web portal students get acknowledged with specific practiced-commands list and adapted teaching tone. Shop shows friendly locked message with Aura balance teaser before Module 6. Progressive disclosure is complete across all touch points.
  </done>
</task>

</tasks>

<verification>
- CLAUDE.md Section 8b has progressive disclosure logic (locked responses, unlock celebrations, status filtering)
- CLAUDE.md Section 14 has enhanced web portal recognition (acknowledgment, teaching adjustments)
- CLAUDE.md Key Commands table shows lock-state behavior for skills/shop/sandbox
- docs/claude/game-systems.md has "Feature Unlock Schedule" with all 3 features documented
- docs/claude/shop-system.md has "Locked State" subsection with message template
- Locked features show encouraging message with unlock condition
- Unlock celebrations fire on module completion or level-up
- Status displays filtered based on unlock flags
</verification>

<success_criteria>
Students see features only when they have context to understand them. Typing "skills" before Module 3 shows a friendly locked message. Completing Module 3 triggers an unlock celebration. Web portal students hear "You already conquered the web portal!" with specific command acknowledgments and adapted Module 1 teaching tone. The experience feels progressive, not restricted.
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-a-flow/02-03-SUMMARY.md`
</output>
