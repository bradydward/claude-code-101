---
phase: 06-global-learning-intelligence
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - web/terminal.html
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Question sync script loads when terminal.html opens"
    - "questionSync.syncQuestion() callable from browser console"
    - "Cloud sync executes when consent given and Supabase configured"
  artifacts:
    - path: "web/terminal.html"
      provides: "Script tag for question-sync.js"
      contains: "question-sync.js"
  key_links:
    - from: "web/terminal.html"
      to: "web/js/question-sync.js"
      via: "script tag"
      pattern: "src=.*question-sync\\.js"
---

<objective>
Wire cloud sync script into web portal.

Purpose: Gap 1 from 06-VERIFICATION.md - question-sync.js exists (240 lines) but is not imported in terminal.html, preventing cloud sync from functioning.

Output: terminal.html imports question-sync.js, enabling cloud sync for students with consent.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-global-learning-intelligence/06-VERIFICATION.md

# Existing files
@web/terminal.html
@web/js/question-sync.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add question-sync.js script tag to terminal.html</name>
  <files>web/terminal.html</files>
  <action>
Add script tag for question-sync.js AFTER privacy-consent.js (line 350) in web/terminal.html.

The script order matters:
1. howler.min.js (audio library)
2. music-system.js (depends on Howler)
3. privacy-consent.js (consent checks)
4. question-sync.js (depends on privacy consent) <-- ADD THIS
5. avatar-system.js
6. terminal-sim.js

Insert this line after the privacy-consent.js script tag (around line 350):
```html
<script src="js/question-sync.js"></script>
```

This wires the existing QuestionSyncManager (240 lines, already exports to window.questionSync) into the web portal, enabling cloud sync for students with consent.
  </action>
  <verify>
grep "question-sync.js" web/terminal.html
  </verify>
  <done>
terminal.html includes question-sync.js script tag after privacy-consent.js. QuestionSyncManager loads and window.questionSync is available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify sync integration in browser context</name>
  <files>web/terminal.html</files>
  <action>
Verify the wiring is complete by checking:

1. Script tags are in correct order in terminal.html:
   - privacy-consent.js BEFORE question-sync.js
   - question-sync.js BEFORE terminal-sim.js

2. Confirm question-sync.js exports to window:
   - window.QuestionSyncManager (class)
   - window.questionSync (instance)

3. Confirm sync respects consent flow:
   - questionSync.syncQuestion() checks privacyConsent.hasConsent()
   - Falls back to local log if no consent

No code changes needed - this task verifies the wiring from Task 1 is complete.
  </action>
  <verify>
# Verify script order (privacy before question-sync)
grep -n "privacy-consent.js\|question-sync.js" web/terminal.html | head -5

# Verify question-sync exports
grep "window.questionSync" web/js/question-sync.js
  </verify>
  <done>
Script loading order verified. QuestionSyncManager instantiates on page load. Sync respects consent via privacyConsent.hasConsent() check.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. terminal.html contains script tag for question-sync.js
2. Script tag appears AFTER privacy-consent.js
3. Script tag appears BEFORE terminal-sim.js
4. No syntax errors in HTML
</verification>

<success_criteria>
1. question-sync.js loads when terminal.html opens
2. window.questionSync available in browser
3. Cloud sync pathway functional (consent + config = sync works)
4. Gap 1 from 06-VERIFICATION.md resolved
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-learning-intelligence/06-07-SUMMARY.md`
</output>
